<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bulk Search Engine</title>
    <meta name="description" content="A ready-to-use multi-search engine. Copy and paste anything into the search bar to find what you need.">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@500&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif; 
            margin: 0; 
            padding: 0; 
            height: 100vh; 
            display: flex; 
            align-items: center; 
            justify-content: center; 
            overflow: hidden; 
        }
        .bg-custom {
            position: absolute;
            top: 0;
            left: 0;
            height: 100vh; 
            width: 100vw; 
            background-color: white; 
        }
        .gradient-overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            top: 0;
            background: linear-gradient(to right, rgba(79, 79, 79, 0.18) 1px, transparent 1px),
                        linear-gradient(to bottom, rgba(79, 79, 79, 0.18) 1px, transparent 1px);
            background-size: 14px 24px;
            mask-image: radial-gradient(ellipse 80% 50% at 50% 0%, #000 70%, transparent 110%);
        }
        .content {
            position: relative; 
            z-index: 10; 
            text-align: center; 
            padding: 20px; 
            border-radius: 10px; 
            background-color: rgba(255, 255, 255, 0.8); 
        }
        h1 {
            margin-bottom: 10px; 
            font-size: 3.75rem; 
            font-family: 'Inter', 'Inter_Fallback', sans-serif; 
            font-weight: 500; 
        }
        .animate-text-gradient {
            background-image: linear-gradient(to right, #0056b3, #000000); 
            background-size: 200% auto;
            background-position: 0% 50%;
            animation: gradientAnimation 3s ease infinite;
            -webkit-background-clip: text; 
            background-clip: text; 
            -webkit-text-fill-color: transparent;
        }
        @keyframes gradientAnimation {
            0% {
                background-position: 0% 50%;
            }
            50% {
                background-position: 100% 50%;
            }
            100% {
                background-position: 0% 50%;
            }
        }
        form {
            margin-top: 20px;
            text-align: center; 
        }
        input[type="text"] {
            padding: 10px; 
            width: 90%; 
            max-width: 600px; 
            border: 1px solid #ccc; 
            border-radius: 5px;
            transition: border-color 0.3s; 
        }
        input[type="text"]:focus {
            border-color: #0056b3; 
            outline: none; 
        }
        button {
            padding: 10px 15px; 
            background-color: black; 
            color: white; 
            border: none; 
            border-radius: 50px; 
            cursor: pointer; 
            margin-left: 10px;
            margin-top: 10px; 
        }
        button:hover {
            background-color: #333; 
        }
    </style>
</head>
<body>
    <div class="bg-custom">
        <div class="gradient-overlay"></div>
    </div>
    <div class="content">
        <h1 class="animate-text-gradient">Bulk Search Engine</h1>
        <p class="description">Ready-to-use multi-search engine, simply copy and paste anything into the search bar.</p>
        <form action="/results" method="get">
            <label for="search-query" class="sr-only">Enter your search query</label>
            <input type="text" id="search-query" name="query" placeholder="Search for articles, images, or anything else..." required>
            <button type="submit">
                Search <i class="fas fa-arrow-right"></i>
            </button>
        </form>
    </div>
</body>
</html>
