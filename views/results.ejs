<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results for "<%= query %>"</title>
    <link href="https://fonts.googleapis.com/css2?family=Gilroy:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Gilroy', sans-serif;
            background-color: #f9f9f9;
            color: #333;
            margin: 20px;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            color: #0056b3;
            margin-bottom: 20px;
        }
        .result {
            border: 1px solid #0056b3;
            background-color: #fff;
            margin-bottom: 10px;
            padding: 15px;
            border-radius: 5px;
        }
        a {
            text-decoration: none;
            color: #0056b3;
            font-weight: bold;
        }
        a:hover {
            text-decoration: underline;
            color: #003d80;
        }
        form {
            margin-top: 20px;
        }
        input[type="text"] {
            padding: 10px;
            width: 300px;
            border: 1px solid #ccc;
            border-radius: 5px;
            margin-right: 10px;
        }
        button {
            padding: 10px 15px;
            background-color: #0056b3;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        button:hover {
            background-color: #003d80;
        }
    </style>
</head>
<body>
    <h1>Search Results for "<span class="highlight"><%= query %></span>"</h1>

    <h2>Search Again</h2>
    <form action="/results" method="get">
        <input type="text" name="query" placeholder="Enter a new search query" required>
        <button type="submit">Search</button>
    </form>

    <% if (searchEngines.length > 0) { %>
        <% searchEngines.forEach(engine => { %>
            <h2><%= engine.name %></h2>
            <div class="result">
                <a href="<%- engine.url %>" target="_blank">View <%= engine.name %> Results</a>
            </div>
        <% }); %>
    <% } else { %>
        <p>No search results available.</p>
    <% } %>
</body>
</html>
